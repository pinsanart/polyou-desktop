class FlashcardControllerService {
    #container
    #flashcardCollection
    #mediaService

    constructor(container, initialFlashcardCollection, mediaService) {
        this.#container = container
        this.#flashcardCollection = initialFlashcardCollection
    }

    render() {

    }
}